{% block body %}


    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.nom) }}
    {{ form_row(form.prenom) }}
    {{ form_row(form.adress) }}
    {{ form_row(form.parrain) }}


    {{ form_row(form.submit, { 'label': 'Submit me' }) }}
    {{ form_end(form) }}
{% endblock body %}