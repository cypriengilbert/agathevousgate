{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(formPassword, { 'method': 'post', 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
 {{ form_errors(formPassword) }}

<div class="form-group">
    {{ form_label(formPassword.current_password, "Mot de passe actuel", {'label_attr': {'class': ''}}) }}
    {{ form_errors(formPassword.current_password) }}
    {{ form_widget(formPassword.current_password, {'attr': {'class': 'form-control'}}) }}

</div>

<div class="form-group">
    {{ form_label(formPassword.plainPassword.first, "Nouveau mot de passe", {'label_attr': {'class': ''}}) }}
    {{ form_errors(formPassword.plainPassword.first) }}
    {{ form_widget(formPassword.plainPassword.first, {'attr': {'class': 'form-control'}}) }}

</div>
<div class="form-group">
    {{ form_label(formPassword.plainPassword.second, "Confirmez votre mot de passe", {'label_attr': {'class': ''}}) }}
    {{ form_errors(formPassword.plainPassword.second) }}
    {{ form_widget(formPassword.plainPassword.second, {'attr': {'class': 'form-control'}}) }}

</div>    <div>
    <center>    <input type="submit" value="{{ 'change_password.submit'|trans }}" class="btn btn-primary btn-lg active btn_blue" /></center>
    </div>
{{ form_end(formPassword) }}
