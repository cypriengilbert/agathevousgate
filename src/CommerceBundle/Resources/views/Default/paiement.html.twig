{% extends 'base.html.twig' %}


{% block body %}

<body>
    <section>
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <h2 class="section-heading">Votre paiement</h2>

                    <form action="{{ path('charge')}}" method="POST">
                      <script
                        src="https://checkout.stripe.com/checkout.js" class="stripe-button"
                        data-key="pk_test_syF86OI1zS3NV2MTSFh5Ca8l"
                        data-amount="{{ prixtotal }}"
                        data-name="Agathe Vous Gâte"
                        data-description=""
                        data-image="{{ asset('/img/logo-principal-menu.png') }}"
                        data-locale="auto"
                        data-zip-code="true"
                        data-currency="eur">
                      </script>
                    </form>

                    <hr class="primary"><br />
                </div>
            </div>
        </div>

</body>


{% endblock %}
