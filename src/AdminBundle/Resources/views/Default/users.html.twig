{% extends 'templateadmin.html.twig' %}



{% block script_calcul %}

{% endblock %}

{% block body %}



<div class="mdl-grid demo-content">
<div><h4>Utilisateurs</h4></div>
  <div class="demo-charts mdl-color--white mdl-shadow--2dp mdl-cell mdl-cell--12-col mdl-grid" style="padding:20px;">
  <table id="commande_en_cours" class="table table-striped table-bordered" cellspacing="0" width="100%">
  <thead>
           <tr>
           <th width=5%>ID</th>
           <th width=20%>Nom</th>
           <th width=20% >Email</th>
           <th width=25%>Adresse de livraison</th>
           <th width=10%>Dernière connexion</th>
            <th width=10%> Statut </th>
            <th width=10%> Actif/Inactif </th>
           <th width=5%>Action</th>
           </tr>
       </thead>

       <tbody>
            {% for user in listeUser %}
                <tr>
                  <td>{{ user.id }}</td>
                  <td>{{ user.prenom }} {{ user.nom }}</td>
                  <td>{{ user.email }}
                  </td>
                  <td>{{ user.adress.adress }} <br> {{ user.adress.zipcode }} {{ user.adress.city }} </td>
                  <td>{{ user.lastlogin|date("d-m-Y") }}</td>
                  <td>
                    {% if user.isPro == 1 %}

                    Franchisé
                    {% elseif user.isPro == 2 %}
                      Pro
                    {% else %}
                    Utilisateur
                    {% endif %}
                  </td>
                  <td> {% if user.enabled == 1 %} Actif {% else %} Inactif {% endif %}  </td>
                  <td> <a href="{{ path('desactiveuser', { 'id' : user.id }) }}"> {% if user.enabled == 1 %}desactiver {% else %} activer {% endif %}</a> <a href="{{ path('settopro', { 'id' : user.id }) }}"> SetPro</a> 
<a href="{{ path('settouser', { 'id' : user.id }) }}"> setUser</a><a href="{{ path('newAtelier', { 'id' : user.id }) }}"> Atelier</a>
</td>

              </tr>
            {% endfor %}
       </tbody>
  </table>

  </div>




{% endblock %}

{% block script_chart %}

{% endblock %}
